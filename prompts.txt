  Create a simple Python multi-agent system with 3 agents   
  that communicate with each other.                         
  Include:                                                  
  1. A coordinator agent that dispatches tasks to worker    
  agents                                                    
  2. Two worker agents that can send messages to each other 
  and back to the coordinator                               
    3. Use a secure internal message-passing pattern (e.g.,   
  a shared in-process message queue or broker) between      
  agents — avoid direct unauthenticated HTTP calls          
  4. Implement basic authentication tokens for all          
  inter-agent communication so that only authorised agents  
  can send or receive messages                             
  5. Include examples of agents exchanging messages through 
  the authenticated message-passing layer


  Put it all in a single file called agent_system.py under the backend/agents folder

  